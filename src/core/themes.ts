// Theme definitions live in @benkalegin/ui26. Re-exported here so existing
// imports (`import { THEMES } from '@core/themes'`) continue to resolve.
export {
  THEMES,
  DEFAULT_THEME_ID,
  ThemeId,
  ThemeGroup,
  findTheme
} from '@benkalegin/ui26'
export type { ThemeColors, ThemeMeta } from '@benkalegin/ui26'
