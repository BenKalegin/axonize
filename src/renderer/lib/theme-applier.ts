// applyTheme + getActiveTheme implementation lives in @benkalegin/ui26. Re-exported
// here so existing imports (`import { applyTheme, getActiveTheme } from '@/lib/theme-applier'`)
// continue to resolve.
export { applyTheme, getActiveTheme } from '@benkalegin/ui26'
